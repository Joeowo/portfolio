# Portfolio Platform — 功能说明文档

作品集管理与网页生成平台，帮助创作者将作品整理成精美的可分享网页。

---

## 平台现有功能概览

```
用户账号      →  素材库       →  创建作品      →  选择模版     →  生成网页     →  分享链接
注册 / 登录      上传图片/视频    组合内容区块     3 套风格       一键生成 HTML    在线访问
                 文本导入        版本历史                         SEO 设置
                 内容抓取        拖拽排序                         上线/下线
                 AI 翻译
```

---

## 一、用户系统

### 已实现

- **注册 / 登录**：用户名 + 密码注册，JWT Token 认证，无状态会话
- **Token 自动续期**：accessToken 2 小时有效，refreshToken 7 天有效，前端无感刷新
- **个人资料**：支持修改昵称、头像、邮箱、个人简介
- **公开主页**：可通过用户 ID 查看任意用户的公开信息
- **角色系统**：`USER`（普通用户）、`ADMIN`、`SUPER_ADMIN` 三级权限

---

## 二、素材库

创作者的私人内容仓库，所有上传和抓取的素材集中管理。

### 已实现

#### 文件上传
- 支持图片（JPG、PNG、WebP、GIF）、视频上传
- 单文件最大 **500MB**
- 文件存储在腾讯云 COS，返回永久可访问的 CDN URL

#### 文件夹管理
- 创建层级文件夹（支持多级嵌套）
- 重命名、删除文件夹
- 素材可归属到指定文件夹

#### 素材操作
- 重命名、添加标签、移动文件夹
- 软删除（不影响已关联作品）
- 按类型（图片/视频/文本）、关键词、文件夹筛选
- 支持排序

#### 文本素材
- 直接创建文本类素材（无需上传文件）
- 从网页抓取导入的文本自动存为文本素材

---

## 三、内容抓取与 AI 翻译

从授权外部网站一键抓取内容，填充到素材库。

### 已实现

#### 网页内容抓取
- 输入目标 URL，自动提取页面内的文字段落和图片
- 单次最多提取 **20 条文本 + 10 张图片**
- 支持勾选需要的内容，选择性导入素材库
- 记录每次抓取历史（URL、提取数量、状态）

#### 授权源站管理
- 用户自行添加可抓取的网站域名（仅限已授权域名可抓取，防止滥用）
- 管理员可添加「全局授权源站」，对所有用户生效
- 支持删除自己添加的源站

#### AI 翻译（DeepSeek）
- 抓取英文内容时，可开启**自动翻译**，中英文对照展示
- 独立翻译接口：单条翻译 / 批量翻译
- 导入时可选择使用原文或译文

---

## 四、作品管理

作品是生成网页前的"草稿"，由多个内容区块组合而成。

### 已实现

#### 作品基本信息
- 标题、简介、封面图（从素材库选取或填写 URL）
- 可见性设置：**私密**（仅自己可见）/ **公开**

#### 内容区块（Sections）

作品由以下 4 种区块自由组合：

| 区块类型 | 说明 |
|---------|------|
| **文本块**（text_block） | 富文本内容，支持 HTML 格式，可加粗、换行等 |
| **图片画廊**（image_gallery） | 多张图片展示，每张可设置说明文字 |
| **视频播放器**（video_player） | 嵌入视频，显示标题 |
| **引言块**（quote） | 突出展示一段引语，带高亮样式 |

区块可设置排列顺序，生成网页时按 order 依次渲染。

#### 版本历史
- 每次保存作品自动生成版本快照
- 可查看历史版本列表（版本号 + 保存时间）
- 支持**一键回滚**到任意历史版本

#### 其他操作
- 复制作品（生成完整副本，包含所有区块）
- 删除作品（软删除，不影响已发布网页）
- 关键词搜索、排序、按可见性筛选

---

## 五、页面模版

决定生成网页的视觉风格，现有 3 套系统内置模版，用户可自定义。

### 系统内置模版（3 套）

| 模版名称 | 布局 | 主色 | 背景 | 适用场景 |
|---------|------|------|------|---------|
| 个人作品集 - 简约白 | 单列居中 | 深蓝 `#1E50A0` | 纯白 `#FFFFFF` | 通用作品集、简历 |
| 项目展示 - 深色风格 | 双栏 | 深色 `#0D1B2A` | 深夜蓝 `#0D1B2A` | 技术项目、暗黑风 |
| 摄影画册 - 网格布局 | 网格 | 深灰 `#2C2C2C` | 浅灰 `#F8F8F8` | 摄影作品、画廊展示 |

### 自定义模版
- 登录用户可创建自己的模版
- 可配置：主题色、背景色、强调色、字体（标题/正文）
- 可配置布局：单列 / 双栏 / 网格
- 支持编辑和删除自己创建的模版

---

## 六、网页生成与发布

将作品 + 模版组合，一键生成可在线访问的静态网页。

### 已实现

#### 生成网页
- 选择已有作品 + 模版，后端自动渲染生成 HTML
- HTML 上传至腾讯云 COS，获得永久访问地址
- 支持设置自定义短链（`pageSlug`，全局唯一）

#### SEO 设置
- 自定义 SEO 标题和描述（写入 `<meta>` 标签）
- 默认使用作品标题和描述
- 发布后仍可修改 SEO 信息

#### 网页访问
- 通过 `GET /api/pages/view/{id}` 直接渲染 HTML（适合 WebView 打开）
- 返回完整的、可直接在浏览器显示的 HTML 页面
- 无需登录即可访问已上线页面

#### 网页管理
- 查看所有已发布网页（访问量、发布时间、当前状态）
- **上线 / 下线**切换（下线后访问接口仍可预览，不对外展示）
- **重新生成**：修改作品内容后可重新渲染，也可切换模版重新生成
- 删除（软删除）

#### 生成的网页包含
- 完整响应式布局，适配手机和桌面
- 内联 CSS，无外部依赖，打开即用
- 根据模版配置自动应用配色和字体
- 页眉（标题 + 描述）、正文区块、页脚

---

## 七、管理后台

面向管理员的管控功能。

### 已实现

| 功能 | 说明 |
|------|------|
| 数据仪表盘 | 平台总作品数、素材数、模版数、在线页面数 |
| 网页审核 | 查看所有用户的发布页面，可强制上线/下架 |
| 系统模版管理 | 创建/上架/下架系统内置模版 |
| 全局授权源站 | 添加对所有用户生效的可抓取域名 |

---

## 技术架构

| 层级 | 技术 |
|------|------|
| 后端 | Spring Boot 3.2.5 + MyBatis-Plus |
| 数据库 | MySQL（JSON 列存储区块内容） |
| 缓存 | Redis |
| 文件存储 | 腾讯云 COS（CDN 加速） |
| AI 翻译 | DeepSeek API |
| 内容抓取 | Jsoup |
| 认证 | JWT（无状态） |
| 前端（Web） | Vue 3 + TypeScript + Element Plus |

---

## 当前数据快照（2026-05-21）

| 项目 | 数量 |
|------|------|
| 系统模版 | 3 套 |
| 测试作品 | 3 个 |
| 测试素材 | 20 个 |
| 已发布在线页面 | 3 个 |

### 可访问的测试页面

```
http://<服务器IP>:8081/api/pages/view/1   城市之光 · 陈艺术家摄影作品集
http://<服务器IP>:8081/api/pages/view/2   自然之境 · 风光摄影集
http://<服务器IP>:8081/api/pages/view/3   城市脉络 · 建筑设计作品集
```

---

## 尚未实现的功能

以下功能在需求文档中规划但当前版本**未实现**：

- 短信验证码注册
- 视频缩略图自动提取
- 断点续传（大文件上传）
- 作品访问量统计（visitCount 字段存在但未计数）
- 网页自定义域名绑定
