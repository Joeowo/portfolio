# Portfolio Platform — 前端项目领域上下文

---

## 项目定位

**Portfolio Platform 前端** 是作品集管理与网页生成平台的前端视觉原型 Demo。

面向 iOS 客户端开发者的后端接口已完整实现（见 README.md），前端项目聚焦于 UI/UX 展示，使用 Mock 数据模拟交互。

---

## 术语表

### 核心概念

| 术语 | 定义 | 备注 |
|------|------|------|
| **Demo / 视觉原型** | 展示产品 UI/UX 的原型版本，数据可以是 Mock 的 | 区别于 MVP（功能验证）或生产版本 |
| **Module / 模块** | 功能单元，对应一组相关页面和组件 | 本项目有 6 个模块 |
| **Nothing 风格** | 基于 Nothing 产品语言的设计系统，强调单色调、字体驱动、精密感 | 参考 `skills/nothing-design` |
| **Hero 元素** | 屏幕中视觉权重最高的元素，用户第一眼看到的内容 | 遵循"三层层级原则" |
| **惊喜时刻** | 每屏中打破常规模式的唯一视觉元素 | 如点阵文字、圆形象征、超大数字 |

### 模块划分

| 模块 | 英文标识 | 职责 |
|------|----------|------|
| 认证模块 | `auth` | 用户登录、注册 |
| 素材库 | `studio/assets` | 素材上传、管理、文件夹组织 |
| 作品编辑器 | `studio/works` | 作品创建、区块编辑、版本管理 |
| 模版选择 | `publish/templates` | 模版浏览、预览、选择 |
| 网页管理 | `publish/pages` | 已发布网页列表、预览、管理 |
| 管理后台 | `admin` | 数据仪表盘、用户管理、审核 |

### 视觉术语

| 术语 | 定义 |
|------|------|
| **点阵背景** | 装饰性网格点阵，暴露系统结构，"结构即装饰" |
| **机械感控件** | 方形、硬朗、精密的控件样式，无阴影无渐变 |
| **全大写标签** | Space Mono 字体、全大写的标签/元数据样式 |
| **Pill 按钮** | 完全圆角（999px）的主要操作按钮 |
| **Technical 按钮** | 4px 小圆角的次要操作按钮 |
| **Hero 数字** | 使用 Doto 字体的超大数字显示 |

### 布局术语

| 术语 | 定义 |
|------|------|
| **混合布局** | 不同页面使用不同布局策略（创作页面全屏，管理页面侧边栏） |
| **沉浸式布局** | 无侧边栏的全屏布局，内容区占据全部空间 |
| **侧边抽屉** | 从右侧滑出的编辑面板，不遮挡主视图 |
| **边缘锚定** | 重要元素固定在屏幕边缘的布局方式 |

---

## 分组路由结构

```
/auth          — 认证流程
/studio        — 创作工作区
  /assets      — 素材库
  /works       — 作品编辑器
/publish       — 发布管理
  /templates   — 模版选择
  /pages       — 网页管理
/admin         — 管理后台
```

---

## 三层层级原则

每屏有且仅有三层视觉重要性：

| 层级 | 内容 | 字体 | 间距 |
|------|------|------|------|
| Primary | 唯一重点（数字/标题） | Doto / Space Grotesk Display | 48-96px |
| Secondary | 支持上下文 | Space Grotesk Body | 8-16px |
| Tertiary | 元数据、导航 | Space Mono Caption（ALL CAPS） | 推至边缘 |

**测试：** 眯眼查看屏幕，能否识别最重要元素？

---

## 设计哲学摘要

1. **减法而非加法** — 每个元素必须证明其存在的必要性
2. **结构即装饰** — 暴露网格、数据、层次本身
3. **单色调画布** — 颜色是事件，不是默认
4. **字体承担重任** — 尺寸、字重、间距创造层级
5. **精密系统感** — 4px 基数、小圆角、细边框、无阴影
6. **机械而温暖** — 技术精确，但有人性温度
7. **一个惊喜时刻** — 每屏一个打破常规的元素

---

## 上下文边界

本文档**不包含**：
- 实现细节（组件代码、API 端点、文件路径）
- 技术选型理由（见 ADR）
- UI 设计规范（见设计系统文档）
- 开发流程（见任务列表）

本文档**仅包含**：
- 领域术语定义
- 概念关系
- 业务规则
- 设计原则

---

## 相关文档

- `README.md` — 后端 API 参考
- `FEATURES.md` — 功能说明文档
- `docs/adr/` — 架构决策记录
- `skills/nothing-design/` — Nothing 设计系统规范
